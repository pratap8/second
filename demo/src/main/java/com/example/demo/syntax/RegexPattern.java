package com.example.demo.syntax;

import java.util.Arrays;
import java.util.List;

public class RegexPattern {

	 public static void main(String args[]){

	        // Following line prints "true" because the whole
	        // text "geeksforgeeks" matches pattern
	        // "geeksforge*ks"
//		 long hours = TimeUnit.HOURS.toDays(40); // 48
//		 Optional str = Optional.empty();
//		 System.out.println(str.isEmpty());
//		 IntStream.of(1,2,3,5,6,7).filter((var i)->i%2==0).forEach(System.out::println);
		 
	    }
}
